name: CI Advanced Check

on: [push]

env:
  APP_ENV: production

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Print environment
        run: |
          echo "Environment is $APP_ENV"

      - name: Conditional step
        if: env.APP_ENV == 'production'
        run: echo "Running production-only checks"

      - name: Simulate test
        run: |
          echo "Running tests..."
          exit 0

